<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodMate">

        <!-- Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Splash -->
        <activity
            android:name="activity.SplashActivty"
            android:exported="false"
            android:theme="@style/Theme.FoodMate.Splash" />

        <!-- Auth -->
        <activity
            android:name=".AuthActivty"
            android:exported="false"
            android:theme="@style/Theme.FoodMate.Auth" />

        <!-- Home -->
        <activity
            android:name="activity.HomeActivty"
            android:exported="false" />

        <!-- Menu -->
        <activity
            android:name="activity.MenuActivity"
            android:exported="false" />

        <!-- Detail -->
        <activity
            android:name="activity.DetailActivity"
            android:exported="false" />

        <!-- Cart -->
        <activity
            android:name="activity.CartActivity"
            android:exported="false" />

    </application>

</manifest>
